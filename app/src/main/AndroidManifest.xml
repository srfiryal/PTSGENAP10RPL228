<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ptsgenap10rpl228">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PTSGENAP10RPL228">
        <activity android:name=".activity.OurPicksActivity"></activity>
        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".model.ControlClass" />
        <activity android:name=".activity.DashboardActivity" />
        <activity android:name=".activity.RegistrationActivity" />
        <activity android:name=".activity.LandingPageActivity" />
        <activity android:name=".activity.LoginActivity" />
    </application>

</manifest>